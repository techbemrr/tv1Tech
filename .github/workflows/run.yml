name: TradingView Scraper A1

on:
  schedule:
    - cron: "30 11 * * 1-5"
  workflow_dispatch:

jobs:
  scrape:
    strategy:
      matrix:
        batch: [0, 1, 2, 3, 4]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - run: npm ci

      - name: Create .env
        run: |
          echo "SHEET_ID=${{ secrets.SHEET_ID }}" >> .env
          echo "SOURCE_SHEET=${{ secrets.SOURCE_SHEET }}" >> .env
          echo "OUTPUT_SHEET_ID=${{ secrets.OUTPUT_SHEET_ID }}" >> .env
          echo "OUTPUT_SHEET=${{ secrets.OUTPUT_SHEET }}" >> .env
          echo "GOOGLE_SERVICE_ACCOUNT_EMAIL=${{ secrets.GOOGLE_SERVICE_ACCOUNT_EMAIL }}" >> .env
          echo "GOOGLE_PRIVATE_KEY=\"${{ secrets.GOOGLE_PRIVATE_KEY }}\"" >> .env
          echo "ACCOUNT_START=0" >> .env
          echo "ACCOUNT_END=450" >> .env
          echo "BATCH_SIZE=90" >> .env

      - name: Restore cookies.json
        run: echo "${{ secrets.COOKIES_BASE64 }}" | base64 -d > cookies.json

      - name: Run Scraper
        run: node index.js ${{ matrix.batch }}

      # ✅ THIS IS WHERE YOUR SCREENSHOTS ARE SAVED
      - name: Archive Debug Screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: screenshots-batch-${{ matrix.batch }}
          path: debug/*.png
          retention-days: 1name: TradingView Scraper A1

on:
  schedule:
    - cron: "30 11 * * 1-5"
  workflow_dispatch:

jobs:
  scrape:
    strategy:
      matrix:
        batch: [0, 1, 2, 3, 4]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20

      - run: npm ci

      - name: Create .env
        run: |
          echo "SHEET_ID=${{ secrets.SHEET_ID }}" >> .env
          echo "SOURCE_SHEET=${{ secrets.SOURCE_SHEET }}" >> .env
          echo "OUTPUT_SHEET_ID=${{ secrets.OUTPUT_SHEET_ID }}" >> .env
          echo "OUTPUT_SHEET=${{ secrets.OUTPUT_SHEET }}" >> .env
          echo "GOOGLE_SERVICE_ACCOUNT_EMAIL=${{ secrets.GOOGLE_SERVICE_ACCOUNT_EMAIL }}" >> .env
          echo "GOOGLE_PRIVATE_KEY=\"${{ secrets.GOOGLE_PRIVATE_KEY }}\"" >> .env
          echo "ACCOUNT_START=0" >> .env
          echo "ACCOUNT_END=450" >> .env
          echo "BATCH_SIZE=90" >> .env

      - name: Restore cookies.json
        run: |
          echo "${{ secrets.COOKIES_BASE64 }}" | base64 -d > cookies.json
          echo "Cookie file created. Size: $(wc -c < cookies.json) bytes"

      - name: Run Scraper
        run: node index.js ${{ matrix.batch }}

      # ✅ THIS STEP SAVES THE SCREENSHOTS
      - name: Upload Debug Screenshots
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: debug-screenshots-batch-${{ matrix.batch }}
          path: "*.png"
          retention-days: 1

